name: Playwright Tests

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch: {}  # Permite ejecución manual

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout del repositorio
        uses: actions/checkout@v4

      - name: 🏗️ Instalar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: 📦 Instalar dependencias
        run: npm install

      - name: 🔽 Instalar Playwright Browsers
        run: npx playwright install --with-deps

      - name: 🚀 Ejecutar pruebas de Playwright
        id: test
        run: |
          npx playwright test --reporter=html,json | tee playwright-output.txt
          echo "PASSED_COUNT=$(grep -oE '[0-9]+ passed' playwright-output.txt | awk '{print $1}')" >> $GITHUB_ENV
          echo "FAILED_COUNT=$(grep -oE '[0-9]+ failed' playwright-output.txt | awk '{print $1}')" >> $GITHUB_ENV
          echo "SKIPPED_COUNT=$(grep -oE '[0-9]+ skipped' playwright-output.txt | awk '{print $1}')" >> $GITHUB_ENV

      - name: 📤 Subir reporte de Playwright como artefacto
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/

      - name: 📢 Notificar resultados en Google Chat
        run: |
          PASSED_COUNT=${{ env.PASSED_COUNT }}
          FAILED_COUNT=${{ env.FAILED_COUNT }}
          SKIPPED_COUNT=${{ env.SKIPPED_COUNT }}
          TOTAL_COUNT=$((PASSED_COUNT + FAILED_COUNT + SKIPPED_COUNT))

          ARTIFACT_URL="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}/artifacts"
          if [ "$FAILED_COUNT" -eq 0 ]; then
            STATUS_MESSAGE="**Resultado de pruebas Nuevo TRUMP**\n✅ Todas las pruebas pasaron exitosamente ($PASSED_COUNT/$TOTAL_COUNT). [📄 Descargar evidencias acá]($ARTIFACT_URL)"
          else
            STATUS_MESSAGE="**Resultado de pruebas Nuevo TRUMP**\n❌ Algunas pruebas fallaron ($PASSED_COUNT/$TOTAL_COUNT). Revisa los logs. [📄 Descargar evidencias acá]($ARTIFACT_URL)"
          fi

          curl -X POST -H 'Content-Type: application/json' \
          -d "{\"text\": \"$STATUS_MESSAGE\"}" \
          "https://chat.googleapis.com/v1/spaces/AAAAOzKWJJQ/messages?key=AIzaSyDdI0hCZtE6vySjMm-WEfRq3CPzqKqqsHI&token=YFmBV6rljIYSHXdLP2ozpebmaaYRbJFCA-8HezJrGPs"
